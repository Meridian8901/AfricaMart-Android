{
  "name": "africamart-android",
  "version": "1.0.0",
  "private": true,
  "description": "AfricaMart — Android wrapper (Capacitor) for the web marketplace",
  "scripts": {
    "open": "cap open android",
    "sync": "cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
