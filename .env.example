# Copy the values you want to change into .env.local (gitignored) to point a
# local dev build at a different backend (e.g. a staging Supabase project),
# without editing the committed .env defaults. See PROJECT_OVERVIEW.md §7.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
EXPO_PUBLIC_TURNSTILE_SITE_KEY=
EXPO_PUBLIC_API_BASE_URL=
